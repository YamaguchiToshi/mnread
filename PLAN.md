# MNREAD-J / Jk Web解析アプリ 実装・検証計画

対象: 自施設・院内用ツール／TypeScript + Vite + React／測定データは端末に保存せず明示的なファイル書き出しのみ

参照: `deep-research-report-1.md`（算出仕様）、`deep-research-report-2.md`（検証仕様）、試作 https://github.com/YamaguchiToshi/mnread

---

## 0. この計画の狙い

試作で「動くもの」は既にできている。今回作るのは、**同じ記録用紙を2人のORTが入力したら必ず同じ値が出て、その値がどの算出法によるものか出力に必ず残る**アプリである。臨床値そのものより、値の再現性と由来の追跡可能性を先に固める。

そのため、フェーズの順序は「計算エンジンを検証で固めてからUIを作る」とする。UIから作ると、計算の誤りがUIの見栄えに隠れて発見が遅れる。

---

## 1. 試作から先に潰すべき差分

試作 `prototype/app.js` の `computeAll()` は、式そのものは公式マニュアルどおりだが、以下5点で調査報告の規定と食い違う。いずれも**出力される臨床値が変わる**。Phase 0〜1でこれを裁定する。

| # | 項目 | 試作の挙動 | 規定 | 影響 |
|---|---|---|---|---|
| 1 | **M値換算** | `M = 10^L` | `M = 10^(L−0.4)`（**Q&A A7 が明記**。マニュアル §4.3「1.1 logMAR = 5M」・§1.2「4M ≒ 28pt」と一致）。0.6 logMAR で 3.98M 対 1.59M | 電子カルテ文の「新聞(1M)に対し約N倍拡大」を**約2.5倍過大**に伝える |
| 2 | pt換算 | `1M ≈ 10pt` のヒューリスティック | 公式式 `pt(L) = 1908·tan(10^L · 5/60 · π/180)`（**Q&A A4.3**、9桁の公式表あり）。0.6 logMAR = 39.8pt 対 11.05pt | 支援現場に渡す推奨文字サイズが実際と乖離 |
| 3 | MRSのプラトー | 最大cpmの90%以上の行を平均 | CPS以上の行の速度の算術平均。プラトーはCPSと一体で決まる | CPS未満の行が混入し、MRSが過小になりうる |
| 4 | CPS | MRSの85%を初めて下回る直前、の1方式のみ | 目視を主値とし、SDev法・指数フィット(80/90/95%)を参考値として併記。値には必ず方法IDを添える | 「CPS 0.6 logMAR」単独では研究・症例報告に使えない |
| 5 | 内部丸め | 補正後logMARとRAを2桁に丸めてから使用 | 内部はフル精度、表示時のみ丸める | 丸め誤差が下流の判定に伝播する |

加えて、入力検証に2点の欠落がある。

- エラー数が n0 を超えた場合に `Math.max(0, cpm)` で黙って0に丸めている → **入力エラーとして拒否すべき**。
- 「不読（0 cpm）」と「未測定（欠測）」が区別されていない → 曲線フィットとMRS/CPSで扱いが変わるため、状態として分離する（時間未入力の行が $N$・$E$ から静かに脱落する問題も同根）。

> **訂正（Phase 0 で判明）**: 当初この表に挙げていた「RAの読了数 N の数え方」は差分ではなかった。全文字を読み損じた項目を $N$ と $E$ の双方に算入しても、除外しても、RA は厳密に一致する（$\Delta RA = -0.1 + 0.1n_0/n_0 = 0$）。試作の挙動は正しく、修正不要。この不変性は逆にプロパティテストとして使う（SPEC §5.3.3）。

---

## 2. アーキテクチャ

```
packages/
  core/                 純粋TS・DOM非依存・全関数が副作用なし
    variants.ts           J = 30文字/分母300、Jk = 24文字/分母240、標準距離30cm
    items.ts              項目の状態モデル（下記）と入力検証
    speed.ts              cpm算出
    acuity.ts             RA・距離補正
    plateau/
      manual.ts             検者が選択した点集合 → MRS/CPS
      sdev.ts               1.96SD反復拡張
      expdecay.ts           指数減衰フィット → CPS80/90/95
    convert.ts            logMAR → M値・pt・小数視力
    report.ts             解析結果オブジェクトの組み立て
  fixtures/             ゴールデン / 境界値 / 合成曲線（JSON、seed固定）
apps/
  web/                  React + 自前SVGチャート + 印刷CSS
```

**設計上の固定制約**

- `core` は日付・乱数・ロケール・DOM に一切触れない。同じ入力から常に同じ出力。
- **UIには計算を一行も書かない。** 表示に必要な数値はすべて `core` の返り値から取る。試作の差分の多くはUIと計算が混ざったことに起因している。
- 解析結果オブジェクトには常に `algorithm_version`・各値の `method`・`quality_flags` を含める。値だけを返す関数を作らない。
- 項目の状態は6値で持つ: `read` / `attempted_unread` / `presented_time_missing` / `unpresented_after_stop` / `skipped_large_assumed_readable` / `skipped_large_unreadable`。真偽値の組み合わせで表現しない。

**技術選択の理由**

- **グラフは自前SVG**（Chart.js等を使わない）: CDN依存をなくしオフラインを担保する／印刷時にベクタのまま出る／CPS・MRSのドラッグ操作のヒットテストを完全に制御できる。描画自体は散布図＋2本の直線であり、ライブラリを入れるほどの複雑さはない。
- **PDFは `window.print()` + `@media print`**: SVGがベクタのまま印刷される。`html2canvas` 系はラスタ化するため、A4レポートの文字が滲む。ブラウザの「PDFとして保存」で目的は足りる。
- **PWA**: Vite PWA plugin。全アセットをバンドルし、CSPで外部リクエストをゼロに固定（PHIの外部送信を設定ミスで起こせない状態にする）。

---

## 3. フェーズ計画

### Phase 0 — 仕様凍結 ✅ 完了（2026-07-31）

3つの文書（既存の基本仕様書・調査報告1・調査報告2）が別々に存在している状態を解消した。

- `SPEC.md` を単一の正として作成。各式に公式マニュアル／Q&A の参照箇所を付した。
- 曖昧点の裁定をADR 10件として記録。
- 型定義（`packages/core/src/types.ts`）を確定。以降のフェーズはこの型に対して実装する。
- **一次資料（`oda.lab/`）を入手し、全式を原典に対して数値検証した。** 調査報告経由ではなく原典が正本になった。
- 原典の図表から `packages/fixtures/` にゴールデンデータを転記し、独立の参照式による転記検証（`pnpm verify:fixtures`）を通した。

検証結果: pt換算は Q&A A4.3 の公式表21件と最大相対誤差 2.8×10⁻⁹、表A（距離補正33件）・表B（小数視力）・表C（速度）は表示桁で完全一致、マニュアル §4 の測定例（19行）は RA・CPS・MRS・M値のすべてが原典の記載値を再現した。

副産物として **OPEN-1（M値の定数0.4）が解決**し、逆に試作の `M = 10^L` が約2.5倍の過大評価であることが確定した。

成果物: `SPEC.md`、`docs/adr/*.md` ×10、`packages/core/src/types.ts`、`packages/fixtures/`

### Phase 1 — 計算コア + ゴールデンテスト ✅ 完了（2026-07-31）

速度・距離補正・読書視力・単位換算・入力検証・読み材料の状態解決。曲線推定には手を付けていない。

実装: `speed.ts` / `distance.ts` / `items.ts` / `acuity.ts` / `convert.ts` / `validation.ts`

検証: **246件すべて通過**（`pnpm test`）
- 原典の公表表に対するゴールデン176件: Q&A A4.3 ポイント表（21）、マニュアル表A 距離補正（33×2）、表B 小数視力（40）、表C 速度（54×2）、チャート M size（19）
- マニュアル §4 測定例 13件: RA・CPS・M値・打ち切り判定・RA の不変性
- 境界値 37件: B01〜B12・B18〜B20（B13〜B17 は曲線に関するもので Phase 2）
- プロパティ 20件（fast-check、各500ケース）: 単調性、距離補正の対称性、一般形と公式形の一致、状態解決の排他性

**変異テストで検出力を確認済み。** 定数や符号を意図的に壊すと確実に失敗する。

| 変異 | 失敗件数 |
|---|---|
| M値オフセット 0.4 → 0.523（当初疑った30cm基準説） | 22 |
| J の定員 30 → 24 | 67 |
| ポイント係数 1908 → 1900 | 21 |
| 距離補正の符号反転 | 42 |
| RA の誤り項を減算に | 7 |

**ゲート達成: Phase 2 に進んでよい。**

> Phase 1 から外し Phase 2 に送ったもの: アクセシビリティ指標（SPEC §5.6）。曲線全体と欠測の補完規則を要するため、プラトー探索と同じ層で扱う。

### Phase 2 — プラトー / CPS推定層 ✅ 完了（2026-07-31）

実装: `curve.ts` / `mrs.ts` / `plateau/{manual,sdev,expdecay}.ts` / `optimize.ts`（自前 Levenberg–Marquardt）/ `accessibility.ts` / `quality.ts` / `analyze.ts`

検証: **325件すべて通過**。合成曲線15族 × 各40本。

| 受け入れ基準 | 結果 |
|---|---|
| 誤った CPS を無警告で出さない（全15族） | **0件** |
| 原典 §4 測定例の目視判定を再現 | プラトー・CPS・MRS すべて一致 |
| clean 曲線の CPS 復元（±0.1 logMAR） | 100% |
| 異常曲線のレビュー要求 | 全該当族で ≥95% |
| clean 曲線の誤発火 | 0% |

**設計を2度作り直した。** 当初の「1.96SD の内部凝集性＋外部分離」は、n点すべてが1.96SDに収まる確率が約0.95ⁿ であるため長いプラトーを構造的に棄却した。次の「最大プラトー速度の80%」は変異テストでSD項が全く効いていないことが露見し、批判したはずの試作の固定比率規則と同型だった。最終形は**患者自身のばらつきから帯を決め、区間全体で再推定する**設計で、変異テスト6件すべてが検出される。

指数減衰フィットは、鋭い two-limb 曲線には原理的に適合しないため残差RMSEで推定不能と明示する（適合しない値を出さない）。

> 未達: `quantised_timing` 族（ノイズ＋0.1秒量子化）の発火率30%。報告書2の目安20%を超えるが、clean 曲線は0%であり、この族は「clean な典型曲線」に当たらない。過剰警告は安全な失敗方向のため、閾値較正は合成データではなく実測で行う（SPEC OPEN-6、Phase 5）。

### Phase 2（当初計画）— プラトー / CPS推定層（約2週）

3方式を並列に実装し、いずれも「推定不能」を正しく返せることを重視する。

- `manual`: 検者が選択した点集合を受け取る純関数。臨床の主値はこれ。
- `sdev`: 平均−1.96SD による反復拡張。探索開始位置・同点規則・欠測の飛び越し可否を仕様に明記した上で実装。
- `expdecay`: 指数減衰フィット → CPS80/90/95。0 cpmの点は対数回帰から除外し、除外件数を返す（εで置換しない）。

フィット結果には収束・残差RMSE・パラメータ境界張り付き・外挿の有無・プラトー観測の有無を必ず含める。CPSが実測範囲外なら「外挿推定」と明示し、通常値として表示しない。

検証: 合成曲線15ファミリー（clean two-limb、ノイズ、緩やかな低下、外れ値、大文字側の低下、二重プラトー、打ち切り、疎な曲線など。seed固定）。
- clean曲線で CPS が真値の±0.1 logMAR以内に入る割合 ≥ 95%
- 異常曲線の ≥ 95% が想定どおりレビュートリガを発火する
- 正常な曲線でレビューが誤発火する割合 < 20%

> 実務投入を早めたい場合、`expdecay` はPhase 5以降に回し、`manual` + `sdev` だけでPhase 3に進む選択肢がある。指数フィットは「参考値の1つ」なので、臨床運用開始の必須条件ではない。

### Phase 3 — 入力UI ✅ 完了（2026-07-31）

実装: `apps/web/`（Vite + React 19 + 自前SVG、外部CDN依存なし）

```
src/session/state.ts     ドラフト（打った文字）→ SessionInput の変換、状態遷移
src/session/reducer.ts   操作の適用と Undo/Redo（純関数、React 非依存）
src/session/derive.ts    core 呼び出し口。画面の数値はすべてここを通る
src/format.ts            表示丸め（SPEC §9）。丸めるのはここだけ
src/components/          ScoreSheet / SpeedCurve / SessionHeader / IssueList /
                         LiveSummary / KeyboardLegend
```

検証: **77件すべて通過**（`pnpm --filter @mnread/web test`）。合計 402件（core 325 + web 77）。

| 内容 | 件数 |
|---|---|
| 状態モデル・Undo（DOM非依存の純関数） | 25 |
| 打鍵操作（testing-library で実際のキー入力を再現） | 19 |
| 入力検証の表示 | 12 |
| 曲線の描画点・座標・軸 | 16 |
| 原典 §4 測定例のUI通し再現 | 5 |

**キー割り当て**（テンキーのみで完結）

| キー | 動作 |
|---|---|
| `Enter` | 確定して次の行の時間へ（誤り数は既定0のまま飛ばせる） |
| `+` | 同じ行の誤り数欄へ |
| `−` | 1つ前の欄へ戻る |
| `*` | この行を「不読（0 cpm）」に |
| `/` → `1`〜`6` | 状態メニュー（6状態すべてに到達できる）、`Esc` で取消 |
| `↑` `↓` | 上下の行の同じ欄へ |
| `Ctrl+Z` / `Ctrl+Shift+Z` | Undo / Redo（連続入力は1段にまとめる） |

**設計上の判断**

- **全19行の初期状態を「未提示」にした。** 検査開始前の事実がそれであり、かつこの状態は core の検証を通るため、**検査中いつでも `analyze()` が成立し曲線を出し続けられる**。
- **エラー時、測定点は描き続けるが CPS・MRS・RA は出さない。** 生の測定点はデータであって解析結果ではない。曲線の形が検査中に見えることが臨床上の最大の価値（PLAN 当初計画）なので、他行のタイプミスで消してはならない。一方 ADR-0004 は「部分的な結果を返さない」を要求するため、解析結果側は完全に伏せる。
- **未提示の行でも時間欄だけは開けておく。** そこに打ち始めることで行が「読んだ」に遷移する。閉じると検査を始められない。core との契約（状態と欄の整合）は打鍵の瞬間に満たされる。

**主グラフの軸を対数速度に変更した（ADR-0011、ADR-0009 を置き換え）**

Phase 0 の ADR-0009 は主グラフを線形 cpm と定めていたが、画面確認の過程で前提の取り違えが判明した。原典 **図4 は読書速度そのものを対数目盛で描いており**（図3 の左軸が時間・右軸が速度で、いずれも対数）、ミネソタ大学の原著論文と MNREAD アプリも同じ、曲線フィットが行われる空間とも一致する。

ADR-0009 の事実認定（対数軸は 0 cpm を描けない／原典 図4 が 0.5 logMAR の 0 cpm を落としている）は維持したまま、結論だけを覆した。**0 cpm は軸を捨てる理由ではなく、軸を分けて描く理由だった。** 対数領域の下に分断した帯を設け、原典が落としている点をそこに描いている。

目盛は対数のみとし、線形表示は用意しない。検者ごと・回ごとに異なる形の曲線で目視判定できてしまう状態は、本プロジェクトの第一目的（再現性）を損なうため。

**あわせて「算出」と「作図」で正本の扱いを分ける線引きを明記した。** 算出は日本語版一次資料にのみ従う（ADR-0001 は不変）。作図の設計は日英いずれの文献も参照してよい。SPEC §1.1 が米国公式を「概念の参照のみ」と位置づけているのがこの区別であり、判定基準は「その参照が数値を変えるか」である。

**UI通しの検証で見つかった実装の誤り2件**

1. **MRS を1方式しか表示していなかった**（ADR-0005 違反）。原典 §4 測定例で 412 cpm（算術平均）と 411 cpm（平均時間換算・原典の記載値）に分かれ、UIが前者だけを出していたことが露見した。3方式すべてを方式IDつきで併記するよう修正。
2. **入力中にエラーが出た瞬間、入力欄の焦点が飛んだ。** エラー文の行を条件付きで足すと React が行ごと作り直すため。「0.5秒」と打つと1打鍵目でエラーになり、続きが打てない。エラーの有無によらず同じ要素型で包むよう修正し、回帰テストを追加。

> Vite の `base` は `/mnread/` に設定済み（Pages 差し替えの前提）。開発時のみ CSP の `connect-src` を緩めるプラグインを入れ、出荷物は `connect-src 'none'` のまま外部送信を封じている。

### Phase 3（当初計画）— 入力UI（約2週）

ORTが**検査をしながら**打つことを前提にする。検査後にまとめて転記する設計にしない。

- 19行スコアシート。テンキーのみで完結。時間を入力してEnterで次行の時間へ（誤り数は既定0で飛ばせる）。ここが実測での速度を最も左右する。
- 行状態のトグル（不読／未提示／上位省略）をキーボードから操作可能に。
- 入力と同時にcpm列と曲線が更新される。**曲線の形が検査中に見えることで、その場で再測定を判断できる** — 試作にもある挙動だが、臨床上これが最大の価値なので確実に残す。
- 入力検証を即時表示。誤り数 > n0、時間 ≤ 0 は赤字で拒否し、黙って丸めない。
- 検査の中断・再開（メモリ上）、Undo。
- 小→大の逆順検査にも対応し、その旨を警告表示した上で整列して解析。

### Phase 4 — 判定UIと出力 ✅ 完了（2026-08-01）

実装: 画面を3面（入力／判定／出力）に分割。core に判読ゾーン層を追加。

```
packages/core/src/zones.ts        判読3ゾーン（SPEC §5.8）
apps/web/src/labels.ts            算出法IDの日本語表示名（1か所に集約）
apps/web/src/components/
  JudgementPanel.tsx                目視判定・自動値の採用・上書き理由・除外
  TimeCurve.tsx                     読書時間の副グラフ（原典 図3）
  OutputPanel.tsx                   出力3系統の窓口
apps/web/src/output/
  emrText.ts                        電子カルテ用テキスト
  PatientReport.tsx                 A4 患者・支援者向けレポート
  exportData.ts                     JSON / CSV 書き出し
```

検証: **472件すべて通過**（core 344 + web 128）。

| 内容 | 件数 |
|---|---|
| 判読ゾーン・監査記録（core） | 19 |
| 判定の状態遷移（DOM非依存） | 9 |
| 判定UI（選択・境界移動・除外・上書き） | 15 |
| 出力3系統（EMR文・レポート・JSON/CSV） | 21 |

**「CPS/MRS のドラッグ調整」を CPS 境界のドラッグのみに改めた（ADR-0012）。** SPEC では CPS も MRS もプラトー集合 $P$ の関数であり、両者を独立に動かせる操作を素直に実装すると、**出力の CPS と MRS が別のプラトーを指す状態**を作れてしまう。検者の操作感は変えず、線を掴む操作を「どの実測サイズからプラトーとみなすか」に写像して $P$ を張り直す形にした。MRS の線にはつまみを付けない。

**判読3ゾーンの境界を裁定した（ADR-0013、SPEC §5.8、OPEN-7）。** 原典に規定がない。境界は RA と CPS の2点のみとし、支援余裕 +0.1 logMAR は**ゾーン境界にしない**（$CPS \le L < CPS+0.1$ では実際に最大速度が出ており、「努力」と表示すると事実と食い違う）。余裕は推奨サイズ範囲 $[pt(CPS), pt(CPS+0.1)]$ として別枠に出す。$RA > CPS$ の退化は値を入れ替えず、空のゾーンと `RA_ABOVE_CPS` で返す。

**監査フラグを1つ足した。** 目視判定が自動値と異なるプラトーを指しているのに理由が空なら `OVERRIDE_REASON_MISSING`。出力は止めない（値は正しく、欠けているのは記録である）。

出力3系統:

1. **電子カルテ用テキスト** — 算出法・測定距離・距離補正を必ず含む。CPS の行には算出法IDを同じ行に置き、目視判定が未実施なら「自動値を暫定の主値としている」と文中に明示する。MRS は3方式併記。判読ゾーンは持ち込まない（測定値と算出法で完結させる）。
2. **A4 患者・支援者向けレポート** — 3ゾーン表・推奨サイズ範囲（別枠）・曲線。氏名なし。`window.print()` と `@media print`。
3. **生データの書き出しと読み込み** — JSON / CSV。氏名欄なし。**丸めない**（再解析に使えることが条件）。JSON はファイルから読み戻せる。

**書き出しは往復できなければ意味がない。** 当初の書き出しは値だけを含んでおり、検者の判定（`PlateauSelection`）がどこにも残っていなかった。CPS と MRS はその集合の関数（ADR-0012）なので、読み戻しても「なぜその CPS になったか」を再現できない。`AnalysisResult.manualPlateau` を追加し、`input` と同じく**結果を決めた入力をそのまま返す**ようにした。往復して同一の JSON になることをテストで固定している。

読み込みの規則（SPEC §8.2.3）: 検者の明示操作のみ／壊れたファイルは読める部分だけ入れずに拒否／`items` は実施順・行はチャート順なので**添字ではなく `chartLogMAR` の値で突き合わせる**（小→大の記録も同じ行に着地する）／版が違うファイルは読むが警告する。

**実機で見つかった不具合3件**（jsdom のテストでは出なかった）

1. **CPS 境界のつまみが、境界上の点を覆ってクリックできなくしていた。** SVG の当たり判定は描画順で決まる。つまみを点より先に描くよう変更（縦に長いので掴む場所は十分残る）。DOM 順序の回帰テストを追加。
2. **判定線のラベルが描画域からはみ出していた。** プラトーは図の右端に寄るため、線の右へ素直に置くと切れる。線が右寄りなら内側へ寄せる。
3. **A4 レポートが2ページに割れ、1枚目の下半分が空いていた。** 図が全幅（約186mm）だと `break-inside: avoid` と相まって図が丸ごと2ページ目へ送られる。印刷時の図幅を 132mm に抑え、1枚に収めた。表の右罫線が紙の端で刷り落ちる件も併せて修正。

**プラトーの採用/不採用を塗りの有無で描き分けた。** 当初は色が同じで**大きさだけ**の違いだったため、どの点を選んでいるのかがひと目で読めなかった。塗りつぶし（採用）と白抜き（不採用）の対比は白黒印刷でも色覚特性があっても残る。印の意味を示す凡例を図のそばに置いた。

### Phase 5 — 実測検証（約2〜4週、臨床業務と並走）🔄 進行中（2026-08-01 着手）

**このフェーズは開発側の手元だけでは 1 歩も進まない。** 実測記録も、目視CPSも、患者向け文言の可否も、
すべて臨床側にしかない。したがって着手時の最初の作業は、**医療者が触れる状態を作って依頼を出すこと**であり、
Pages の差し替え（後述）と依頼文（`docs/review-request.md`）がその中身である。

- 自施設の過去記録 20〜30例を、手計算または既存MNJAの値と突き合わせる。
- ORT 2〜3名で、目視CPSと自動値の一致を確認。
- **二重入力テスト**: 同じ記録用紙を2人が独立に入力し、結果が一致するか。入力UIの取りこぼしを最も効率よく検出できる。
- **医療者による監修**。Phase 4 で患者・支援者の手に渡る紙（A4レポート）と、カルテに残る定型文が出るようになった。ここから先は実装の正しさではなく**臨床上の妥当性**が主題であり、開発側だけでは決められない。少なくとも次を見てもらう。
  - 判読3ゾーンの区分と文言（OPEN-7）。とくに「努力」の下端を RA そのものに置いてよいか
  - A4レポートの日本語が、患者・支援者にそのまま渡せる水準か
  - 電子カルテ用テキストの項目立てが、貼り付け先の運用に合っているか
  - 推奨文字サイズの提示（下限とゆとりの2値）が、支援の現場で使える形か

受け入れ基準（院内ツールとしての線引き）:
- 式レベルの値は手計算と完全一致
- CPSは目視値と±0.1 logMAR以内が90%以上
- 乖離した例は**すべて原因を説明できること**（説明できない乖離が1件でも残っている状態でリリースしない）

#### 臨床側を待つあいだに片づけた開発側の宿題（2026-08-01）

SPEC §5.5.2 が Phase 5 に送っていた `mnreadR` 2.1.7 との差異測定を実施し、一件ずつ裁定した
（`docs/mnreadr-comparison.md`、ADR-0014）。**mnreadR に合わせるべき差異は1件もなかった**が、
照合の副産物として実在の欠陥が2件出たので、その場で処理した。

| 所見 | 内容 | 措置 |
|---|---|---|
| E-1（OPEN-8）| 単一の高い外れ値が核を乗っ取り CPS が過小になる。`single_high_outlier` 族 40/40 | 帯の標本から最速1点を除外し、プラトーを再推定の不動点に（ADR-0016）。合成600本の完全一致 457 → 498 |
| F-1（OPEN-9）| 出荷の既定構成が受け入れテストの構成より弱く、silent-wrong が1本すり抜けていた | 既定に `expdecay_90` を追加し、合成テストを出荷構成でも走らせる（ADR-0015） |

変異テストは11件に増え、すべてがテストで検出される。**±0.1 logMAR の精度指標は設計の破綻を
隠しうる**（収束していない反復を「正解」と数えた）ため、収束性を独立に測るようになった。

### GitHub Pages とブランチ構成

共同開発者への完成イメージ提示のため、`https://yamaguchitoshi.github.io/mnread/` で試作版を配信している。**このURLは当面現状を維持する必要がある。**

当初 Pages は `main` 直下から配信されていたため、本命実装を `main` に push するとデモが消える状態だった。これを避けるため、Pages のソースを試作専用のブランチへ切り離した。

| ブランチ | 内容 | Pages |
|---|---|---|
| `main` | 本命実装 | **配信中**（`.github/workflows/pages.yml`） |
| `gh-pages` | 試作版（`07e5ecc` 固定） | 役目を終えた。記録として残す |

**差し替え手順（Phase 4 以降）** — 2026-08-01 実施

1. ✅ Vite の `base` を `/mnread/` に設定する（Pages はサブパス配信のため。Phase 3 で実施済み）
2. ✅ GitHub Actions で `main` からビルドし、Pages へ直接デプロイする方式に切り替える（`build_type` を `legacy` から `workflow` へ）
3. ✅ 切り替え後 `gh-pages` ブランチは役目を終える。試作を残したい場合は削除せずに置いておけばよい
4. 差し替えのタイミングは共同開発者と合わせる

**配信の門**: `pages.yml` は型チェック・転記検証・全テストを通したものだけを配信する。
臨床値を出すツールであり、壊れた計算を配信しないことのほうが、配信が速いことより重要である。

**検索エンジンには載せない（検証中のあいだ）**: `apps/web/index.html` に
`<meta name="robots" content="noindex, nofollow, noarchive">` を置いてある。GitHub Pages は
応答ヘッダを足せないため `X-Robots-Tag` は使えず、robots.txt も効かない — クローラが読むのは
ドメイン直下の `https://yamaguchitoshi.github.io/robots.txt` であり、本リポジトリから配信できる
のは `/mnread/robots.txt` だけだからである。**行儀のよいクローラへのお願いであって閲覧の制限
ではない**（URL を知っていれば誰でも開ける。院外に渡すのは監修依頼の相手に限る運用で担保する）。
`apps/web/test/index-html.test.ts` が消失を検出する。**Phase 5 の検証完了時に外す。**
CI（`ci.yml`）は PR と `main` への push で同じ検証を回す（PLAN §4）。

**公開時の注意**: リポジトリは public であり、Pages に載せた時点で誰でもアクセスできる臨床値計算ツールになる。Phase 5 の実測検証が完了するまでは、画面と全出力に `algorithmVersion` と検証状況を明示すること（Phase 4 の出力仕様に含める）。

### Phase 6 — 配布・運用（約1週）

- PWAとして院内端末にインストール、オフライン動作確認。
- ✅ `algorithm_version` を画面と全出力に表示。後から「この値はどの版で出したか」を追える状態にする。
- ✅ 既知の限界を明記した利用上の注意（Jk→Jの外挿、pt換算はフォント依存、ACCは日本語版の正規化定数が存在しない）。

**うち2件を Phase 5 の待ち時間に前倒しした（2026-08-01）。** 監修依頼を出した時点で、
臨床検証を通していない値が医療者の画面に出ている。「公開時の注意」が求めるのは版と検証状況の
明示であり、これは配布の前ではなく**公開の瞬間から**必要になる。

`apps/web/src/components/UsageNotes.tsx` を追加した。設計上の判断は3つ。

1. **帯は常設で閉じられない。** 検証状況は読み飛ばしてよい情報ではない。閉じられるのは詳細のみ。
2. **「読んだので今後は出さない」を作らない。** 端末に何も保存しない設計（SPEC §8.2.3）では
   そもそも実装できず、無理に入れれば保存しない前提のほうが壊れる。
3. **詳細は既定で畳む。** 検査中の画面を占有させない。`no-print` とし、A4レポートには
   患者向けの注意書き（SPEC §8.2.2）が別にある — 開発者向けの限界一覧を患者に渡す紙に載せない。

記載した限界は、PLAN が挙げる3件（pt のフォント依存、Jk→漢字かな交じり文の外挿、ACC の
正規化定数不在）に加えて、原典に規定がない裁定（OPEN-7 の判読3ゾーン、OPEN-2 の大文字側不読）、
合成データ由来の暫定閾値（OPEN-4・OPEN-6）、MRS 3方式併記の理由（ADR-0005）、
読書視力の表示桁より細かい差が画面に出ないこと（SPEC §9）である。

> 残るは PWA / オフライン動作の確認のみ。

---

## 4. CI構成

| 段階 | 実行内容 |
|---|---|
| コミットごと | 型チェック、core単体、ゴールデン、境界値、プロパティテスト |
| PRごと | 合成曲線一式、出力スナップショット（EMRテキスト・レポートHTML） |
| 週次 | seedを変えた大量合成曲線、複数初期値でのフィット安定性 |

固定するもの: Node版、lockfile、乱数seed、ロケールと小数点、`algorithm_version`、fixtureのチェックサム。

グラフはピクセル比較でテストしない。描画点数・CPS線座標・MRS線座標・除外点数・軸範囲・警告表示の有無をアサートする。

---

## 5. 横断的な留意点

**薬機法・医療機器プログラム該当性** — 本アプリは測定値の計算と表示に留まり、診断・治療の判断を行わない設計とする（推奨倍率・推奨ptは「参考値」表示を維持）。この前提であれば非該当と整理できる見込みだが、**院外配布や販売を検討する段階では改めて確認が必要**。院内ツールに留める限りは設計制約として守ればよい。

**個人情報** — 端末に保存しない設計。氏名欄を設けずIDのみとする。書き出したファイルの取り扱いは院内規程に従う（アプリ側では管理しない）。

**アクセシビリティ** — WCAG 2.1 AA。ただし実質的に重要なのは操作者（ORT）のキーボード完結性と、患者向けレポートの可読性（高コントラスト・大きめの文字）である。

---

## 6. 見積り

合計 **8〜11週**（週10〜15時間想定）。

- Phase 1完了 → 信頼できる計算エンジンが手に入る
- Phase 4完了 → 実務投入可能
- Phase 5完了 → 臨床的に妥当と言える

Phase 2の指数フィットを後回しにする場合、実務投入までを約2週短縮できる。

---

## 7. 着手時の最初の作業

1. `SPEC.md` の作成と、第1節の差分5点の裁定（ADR）。
2. リポジトリ構成の初期化（pnpm workspace + Vite + Vitest）。
3. `packages/core/types.ts` の確定 — 以降すべてがこの型に依存するため、ここだけは先に固める。
4. ルートの `CLAUDE.md` を、この計画とディレクトリ構成に合わせて書き直す（現状は「コードなし」を前提とした記述になっている）。
