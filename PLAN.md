# MNREAD-J / Jk Web解析アプリ 実装・検証計画

対象: 自施設・院内用ツール／TypeScript + Vite + React／測定データは端末に保存せず明示的なファイル書き出しのみ

参照: `deep-research-report-1.md`（算出仕様）、`deep-research-report-2.md`（検証仕様）、試作 https://github.com/YamaguchiToshi/mnread

---

## 0. この計画の狙い

試作で「動くもの」は既にできている。今回作るのは、**同じ記録用紙を2人のORTが入力したら必ず同じ値が出て、その値がどの算出法によるものか出力に必ず残る**アプリである。臨床値そのものより、値の再現性と由来の追跡可能性を先に固める。

そのため、フェーズの順序は「計算エンジンを検証で固めてからUIを作る」とする。UIから作ると、計算の誤りがUIの見栄えに隠れて発見が遅れる。

---

## 1. 試作から先に潰すべき差分

試作 `prototype/app.js` の `computeAll()` は、式そのものは公式マニュアルどおりだが、以下5点で調査報告の規定と食い違う。いずれも**出力される臨床値が変わる**。Phase 0〜1でこれを裁定する。

| # | 項目 | 試作の挙動 | 規定 | 影響 |
|---|---|---|---|---|
| 1 | **M値換算** | `M = 10^L` | `M = 10^(L−0.4)`（**Q&A A7 が明記**。マニュアル §4.3「1.1 logMAR = 5M」・§1.2「4M ≒ 28pt」と一致）。0.6 logMAR で 3.98M 対 1.59M | 電子カルテ文の「新聞(1M)に対し約N倍拡大」を**約2.5倍過大**に伝える |
| 2 | pt換算 | `1M ≈ 10pt` のヒューリスティック | 公式式 `pt(L) = 1908·tan(10^L · 5/60 · π/180)`（**Q&A A4.3**、9桁の公式表あり）。0.6 logMAR = 39.8pt 対 11.05pt | 支援現場に渡す推奨文字サイズが実際と乖離 |
| 3 | MRSのプラトー | 最大cpmの90%以上の行を平均 | CPS以上の行の速度の算術平均。プラトーはCPSと一体で決まる | CPS未満の行が混入し、MRSが過小になりうる |
| 4 | CPS | MRSの85%を初めて下回る直前、の1方式のみ | 目視を主値とし、SDev法・指数フィット(80/90/95%)を参考値として併記。値には必ず方法IDを添える | 「CPS 0.6 logMAR」単独では研究・症例報告に使えない |
| 5 | 内部丸め | 補正後logMARとRAを2桁に丸めてから使用 | 内部はフル精度、表示時のみ丸める | 丸め誤差が下流の判定に伝播する |

加えて、入力検証に2点の欠落がある。

- エラー数が n0 を超えた場合に `Math.max(0, cpm)` で黙って0に丸めている → **入力エラーとして拒否すべき**。
- 「不読（0 cpm）」と「未測定（欠測）」が区別されていない → 曲線フィットとMRS/CPSで扱いが変わるため、状態として分離する（時間未入力の行が $N$・$E$ から静かに脱落する問題も同根）。

> **訂正（Phase 0 で判明）**: 当初この表に挙げていた「RAの読了数 N の数え方」は差分ではなかった。全文字を読み損じた項目を $N$ と $E$ の双方に算入しても、除外しても、RA は厳密に一致する（$\Delta RA = -0.1 + 0.1n_0/n_0 = 0$）。試作の挙動は正しく、修正不要。この不変性は逆にプロパティテストとして使う（SPEC §5.3.3）。

---

## 2. アーキテクチャ

```
packages/
  core/                 純粋TS・DOM非依存・全関数が副作用なし
    variants.ts           J = 30文字/分母300、Jk = 24文字/分母240、標準距離30cm
    items.ts              項目の状態モデル（下記）と入力検証
    speed.ts              cpm算出
    acuity.ts             RA・距離補正
    plateau/
      manual.ts             検者が選択した点集合 → MRS/CPS
      sdev.ts               1.96SD反復拡張
      expdecay.ts           指数減衰フィット → CPS80/90/95
    convert.ts            logMAR → M値・pt・小数視力
    report.ts             解析結果オブジェクトの組み立て
  fixtures/             ゴールデン / 境界値 / 合成曲線（JSON、seed固定）
apps/
  web/                  React + 自前SVGチャート + 印刷CSS
```

**設計上の固定制約**

- `core` は日付・乱数・ロケール・DOM に一切触れない。同じ入力から常に同じ出力。
- **UIには計算を一行も書かない。** 表示に必要な数値はすべて `core` の返り値から取る。試作の差分の多くはUIと計算が混ざったことに起因している。
- 解析結果オブジェクトには常に `algorithm_version`・各値の `method`・`quality_flags` を含める。値だけを返す関数を作らない。
- 項目の状態は6値で持つ: `read` / `attempted_unread` / `presented_time_missing` / `unpresented_after_stop` / `skipped_large_assumed_readable` / `skipped_large_unreadable`。真偽値の組み合わせで表現しない。

**技術選択の理由**

- **グラフは自前SVG**（Chart.js等を使わない）: CDN依存をなくしオフラインを担保する／印刷時にベクタのまま出る／CPS・MRSのドラッグ操作のヒットテストを完全に制御できる。描画自体は散布図＋2本の直線であり、ライブラリを入れるほどの複雑さはない。
- **PDFは `window.print()` + `@media print`**: SVGがベクタのまま印刷される。`html2canvas` 系はラスタ化するため、A4レポートの文字が滲む。ブラウザの「PDFとして保存」で目的は足りる。
- **PWA**: Vite PWA plugin。全アセットをバンドルし、CSPで外部リクエストをゼロに固定（PHIの外部送信を設定ミスで起こせない状態にする）。

---

## 3. フェーズ計画

### Phase 0 — 仕様凍結 ✅ 完了（2026-07-31）

3つの文書（既存の基本仕様書・調査報告1・調査報告2）が別々に存在している状態を解消した。

- `SPEC.md` を単一の正として作成。各式に公式マニュアル／Q&A の参照箇所を付した。
- 曖昧点の裁定をADR 10件として記録。
- 型定義（`packages/core/src/types.ts`）を確定。以降のフェーズはこの型に対して実装する。
- **一次資料（`oda.lab/`）を入手し、全式を原典に対して数値検証した。** 調査報告経由ではなく原典が正本になった。
- 原典の図表から `packages/fixtures/` にゴールデンデータを転記し、独立の参照式による転記検証（`pnpm verify:fixtures`）を通した。

検証結果: pt換算は Q&A A4.3 の公式表21件と最大相対誤差 2.8×10⁻⁹、表A（距離補正33件）・表B（小数視力）・表C（速度）は表示桁で完全一致、マニュアル §4 の測定例（19行）は RA・CPS・MRS・M値のすべてが原典の記載値を再現した。

副産物として **OPEN-1（M値の定数0.4）が解決**し、逆に試作の `M = 10^L` が約2.5倍の過大評価であることが確定した。

成果物: `SPEC.md`、`docs/adr/*.md` ×10、`packages/core/src/types.ts`、`packages/fixtures/`

### Phase 1 — 計算コア + ゴールデンテスト ✅ 完了（2026-07-31）

速度・距離補正・読書視力・単位換算・入力検証・読み材料の状態解決。曲線推定には手を付けていない。

実装: `speed.ts` / `distance.ts` / `items.ts` / `acuity.ts` / `convert.ts` / `validation.ts`

検証: **246件すべて通過**（`pnpm test`）
- 原典の公表表に対するゴールデン176件: Q&A A4.3 ポイント表（21）、マニュアル表A 距離補正（33×2）、表B 小数視力（40）、表C 速度（54×2）、チャート M size（19）
- マニュアル §4 測定例 13件: RA・CPS・M値・打ち切り判定・RA の不変性
- 境界値 37件: B01〜B12・B18〜B20（B13〜B17 は曲線に関するもので Phase 2）
- プロパティ 20件（fast-check、各500ケース）: 単調性、距離補正の対称性、一般形と公式形の一致、状態解決の排他性

**変異テストで検出力を確認済み。** 定数や符号を意図的に壊すと確実に失敗する。

| 変異 | 失敗件数 |
|---|---|
| M値オフセット 0.4 → 0.523（当初疑った30cm基準説） | 22 |
| J の定員 30 → 24 | 67 |
| ポイント係数 1908 → 1900 | 21 |
| 距離補正の符号反転 | 42 |
| RA の誤り項を減算に | 7 |

**ゲート達成: Phase 2 に進んでよい。**

> Phase 1 から外し Phase 2 に送ったもの: アクセシビリティ指標（SPEC §5.6）。曲線全体と欠測の補完規則を要するため、プラトー探索と同じ層で扱う。

### Phase 2 — プラトー / CPS推定層（約2週）

3方式を並列に実装し、いずれも「推定不能」を正しく返せることを重視する。

- `manual`: 検者が選択した点集合を受け取る純関数。臨床の主値はこれ。
- `sdev`: 平均−1.96SD による反復拡張。探索開始位置・同点規則・欠測の飛び越し可否を仕様に明記した上で実装。
- `expdecay`: 指数減衰フィット → CPS80/90/95。0 cpmの点は対数回帰から除外し、除外件数を返す（εで置換しない）。

フィット結果には収束・残差RMSE・パラメータ境界張り付き・外挿の有無・プラトー観測の有無を必ず含める。CPSが実測範囲外なら「外挿推定」と明示し、通常値として表示しない。

検証: 合成曲線15ファミリー（clean two-limb、ノイズ、緩やかな低下、外れ値、大文字側の低下、二重プラトー、打ち切り、疎な曲線など。seed固定）。
- clean曲線で CPS が真値の±0.1 logMAR以内に入る割合 ≥ 95%
- 異常曲線の ≥ 95% が想定どおりレビュートリガを発火する
- 正常な曲線でレビューが誤発火する割合 < 20%

> 実務投入を早めたい場合、`expdecay` はPhase 5以降に回し、`manual` + `sdev` だけでPhase 3に進む選択肢がある。指数フィットは「参考値の1つ」なので、臨床運用開始の必須条件ではない。

### Phase 3 — 入力UI（約2週）

ORTが**検査をしながら**打つことを前提にする。検査後にまとめて転記する設計にしない。

- 19行スコアシート。テンキーのみで完結。時間を入力してEnterで次行の時間へ（誤り数は既定0で飛ばせる）。ここが実測での速度を最も左右する。
- 行状態のトグル（不読／未提示／上位省略）をキーボードから操作可能に。
- 入力と同時にcpm列と曲線が更新される。**曲線の形が検査中に見えることで、その場で再測定を判断できる** — 試作にもある挙動だが、臨床上これが最大の価値なので確実に残す。
- 入力検証を即時表示。誤り数 > n0、時間 ≤ 0 は赤字で拒否し、黙って丸めない。
- 検査の中断・再開（メモリ上）、Undo。
- 小→大の逆順検査にも対応し、その旨を警告表示した上で整列して解析。

### Phase 4 — 判定UIと出力（約2週）

**グラフの軸を試作から変更する。** 主グラフは横軸＝補正後logMAR、縦軸＝読書速度cpm（**線形軸**）。試作および旧仕様書の「読破時間の対数軸」は 0 cpm（全文字誤り）を表現できず、ロービジョン例で最も重要な小文字側の情報が落ちる。読破時間の対数軸表示はマニュアル図3との対照用に副表示として残す。

- プラトー点のクリック選択、CPS/MRSのドラッグ調整、自動値の併記。
- 自動値を上書きした場合は理由を記録し、出力に残す（監査）。
- 出力3系統:
  1. **電子カルテ用テキスト** — CPS・MRS・RAに加え、**算出法・測定距離・距離補正値を必ず含める**。「CPS 0.6 logMAR」単独の行を出力しない。
  2. **A4患者・支援者向けレポート** — 快適／努力／不可の3ゾーン表示、推奨文字サイズを公式pt換算で。CPS相当ptと支援余裕（+0.1 logMAR ≒ 1.26倍）を**分けて**表示する。CPSは「快適さの保証値」ではなく「最大速度を支える最小サイズ」であるため。
  3. **生データ書き出し** — JSON/CSV。氏名欄は設けずIDのみ。

### Phase 5 — 実測検証（約2〜4週、臨床業務と並走）

- 自施設の過去記録 20〜30例を、手計算または既存MNJAの値と突き合わせる。
- ORT 2〜3名で、目視CPSと自動値の一致を確認。
- **二重入力テスト**: 同じ記録用紙を2人が独立に入力し、結果が一致するか。入力UIの取りこぼしを最も効率よく検出できる。

受け入れ基準（院内ツールとしての線引き）:
- 式レベルの値は手計算と完全一致
- CPSは目視値と±0.1 logMAR以内が90%以上
- 乖離した例は**すべて原因を説明できること**（説明できない乖離が1件でも残っている状態でリリースしない）

### Phase 6 — 配布・運用（約1週）

- PWAとして院内端末にインストール、オフライン動作確認。
- `algorithm_version` を画面と全出力に表示。後から「この値はどの版で出したか」を追える状態にする。
- 既知の限界を明記した利用上の注意（Jk→Jの外挿、pt換算はフォント依存、ACCは日本語版の正規化定数が存在しない）。

---

## 4. CI構成

| 段階 | 実行内容 |
|---|---|
| コミットごと | 型チェック、core単体、ゴールデン、境界値、プロパティテスト |
| PRごと | 合成曲線一式、出力スナップショット（EMRテキスト・レポートHTML） |
| 週次 | seedを変えた大量合成曲線、複数初期値でのフィット安定性 |

固定するもの: Node版、lockfile、乱数seed、ロケールと小数点、`algorithm_version`、fixtureのチェックサム。

グラフはピクセル比較でテストしない。描画点数・CPS線座標・MRS線座標・除外点数・軸範囲・警告表示の有無をアサートする。

---

## 5. 横断的な留意点

**薬機法・医療機器プログラム該当性** — 本アプリは測定値の計算と表示に留まり、診断・治療の判断を行わない設計とする（推奨倍率・推奨ptは「参考値」表示を維持）。この前提であれば非該当と整理できる見込みだが、**院外配布や販売を検討する段階では改めて確認が必要**。院内ツールに留める限りは設計制約として守ればよい。

**個人情報** — 端末に保存しない設計。氏名欄を設けずIDのみとする。書き出したファイルの取り扱いは院内規程に従う（アプリ側では管理しない）。

**アクセシビリティ** — WCAG 2.1 AA。ただし実質的に重要なのは操作者（ORT）のキーボード完結性と、患者向けレポートの可読性（高コントラスト・大きめの文字）である。

---

## 6. 見積り

合計 **8〜11週**（週10〜15時間想定）。

- Phase 1完了 → 信頼できる計算エンジンが手に入る
- Phase 4完了 → 実務投入可能
- Phase 5完了 → 臨床的に妥当と言える

Phase 2の指数フィットを後回しにする場合、実務投入までを約2週短縮できる。

---

## 7. 着手時の最初の作業

1. `SPEC.md` の作成と、第1節の差分5点の裁定（ADR）。
2. リポジトリ構成の初期化（pnpm workspace + Vite + Vitest）。
3. `packages/core/types.ts` の確定 — 以降すべてがこの型に依存するため、ここだけは先に固める。
4. ルートの `CLAUDE.md` を、この計画とディレクトリ構成に合わせて書き直す（現状は「コードなし」を前提とした記述になっている）。
