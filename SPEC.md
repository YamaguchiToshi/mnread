# MNREAD-J / Jk 解析仕様書

**SPEC_VERSION: 0.5.0**

本書は MNREAD-J / MNREAD-Jk Web解析アプリの**単一の正**である。実装・テスト・レビューは本書に対して行い、本書と実装が食い違う場合は本書を先に改訂する。

本書を変更した場合は `SPEC_VERSION` を上げ、影響するゴールデンテストを同一コミットで更新する。

---

## 1. 適用範囲と正本

### 1.1 正本の階層

| 順位 | 資料 | 所在 | 本書での扱い |
|---|---|---|---|
| 1 | 小田ら『MNREAD-J, Jk チャートマニュアル』（2002-05-18） | `oda.lab/MNREAD-J-JkMan020518.pdf` | **日本語版採点の規範**。第5章の式はすべてここに帰着する |
| 2 | 小田研究室 MNREAD-J Q&A（1998-2010） | `oda.lab/odalab web resource center.pdf` | 単位換算・採点細則（自己訂正、行飛ばし、視野狭窄） |
| 3 | University of Minnesota 公式（英語版） | — | 概念の参照のみ。**英語版の定数を日本語モードに持ち込まない** |
| 4 | `mnreadR` 2.1.7、Cheung 2008、Baskaran 2019 | — | 任意の自動推定アルゴリズムの出典。参考値であって主値ではない |

**順位1・2は原典が `oda.lab/` にある。** 式に関する疑義は調査報告ではなく原典に当たること。`deep-research-report-1.md` / `-2.md` は二次資料であり、原典と食い違う場合は原典が優先する。

本書の各式は原典に対して数値検証済みである（`packages/fixtures`）。とくにポイント換算は Q&A A4.3 の公式表21件と最大相対誤差 2.8×10⁻⁹ で一致する。

### 1.2 対象

MNREAD-J（漢字かな交じり文・30文字）および MNREAD-Jk（ひらがな単語・24文字）。英語版MNREADは**解析対象に含めない**（`mnreadR` との回帰テスト用に内部的な計算モードを持つ可能性はあるが、UIには出さない）。

### 1.3 位置づけ

自施設内での測定値の計算・表示に用いる。診断・治療の判断を行わない。推奨倍率・推奨文字サイズは参考値として表示する。

---

## 2. 記号

| 記号 | 意味 | 単位 |
|---|---|---|
| $n_0$ | 1読み材料あたりの文字数（J:30 / Jk:24） | 文字 |
| $e_i$ | 読み材料 $i$ の読み損じ文字数 | 文字 |
| $t_i$ | 読み材料 $i$ の読書時間 | 秒 |
| $v_i$ | 読み材料 $i$ の読書速度 | cpm |
| $L_i$ | チャート表示 logMAR | logMAR |
| $L_i^{c}$ | 距離補正後 logMAR | logMAR |
| $D$ | 実測視距離 | cm |
| $D_0$ | 標準視距離（J/Jk とも 30） | cm |
| $N$ | 読んだ、または読もうとした読み材料数 | 個 |
| $E$ | 累積読み損じ文字数 | 文字 |

---

## 3. チャート定義

| 項目 | MNREAD-J | MNREAD-Jk |
|---|---|---|
| 1読み材料 | 30文字（10文字×3行） | 24文字（9語のひらがな分かち書き） |
| 誤り単位 | 文字 | 文字 |
| $n_0$ | 30 | 24 |
| RA分母 | 300 | 240 |
| 標準距離 | 30 cm | 30 cm |
| 段数 | 19段 | 19段 |
| logMAR範囲 | 1.3 → −0.5（0.1刻み） | 同左 |

RA分母は $n_0 / h$（$h = 0.1$ はチャート刻み）に等しい。将来チャートの刻みや上端が変わった場合は $n_0$ と $h$ から導出する（§5.3.2）。

---

## 4. 入力データモデル

### 4.1 セッション

| フィールド | 型 | 必須 | 備考 |
|---|---|---|---|
| `variant` | `MNREAD-J` \| `MNREAD-Jk` | ○ | |
| `chartVersion` | string | ○ | チャート版の識別子 |
| `viewingDistanceCm` | number > 0 | ○ | セッション既定値 |
| `polarity` | `black_on_white` \| `white_on_black` | ○ | |
| `eye` | `right` \| `left` \| `both` | ○ | |
| `sequenceDirection` | `large_to_small` \| `small_to_large` | ○ | 既定は `large_to_small` |
| `subjectId` | string | | **氏名欄は設けない** |
| `age` / `sex` / `testDate` | | | 任意 |
| `items` | ItemInput[] | ○ | 19要素 |

### 4.2 読み材料（項目）の状態

**状態は下記6値のいずれかとし、真偽値の組み合わせで表現しない。** 「0 cpm」と「欠測」を混同しないことが目的である。

| 状態 | 意味 | $N$算入 | $E$寄与 | 速度 | 曲線 |
|---|---|:---:|---|---|---|
| `read` | 提示し、読んだ | ○ | $e_i$（記録値） | $60(n_0-e_i)/t_i$ | ○ |
| `attempted_unread` | 提示し試行したが全く読めなかった | ○ | $n_0$ | **0 cpm** | ○（0として） |
| `presented_time_missing` | 提示し読んだが時間が未記録 | ○ | $e_i$（記録値） | 欠測 | × |
| `unpresented_after_stop` | 検査終了後、提示していない | × | — | 欠測 | × |
| `skipped_large_assumed_readable` | 上位を省略（読めるとみなした） | ○ | 0 | 欠測 | × |
| `skipped_large_unreadable` | 大文字側が読めない（輪状暗点等） | ○ | $n_0$ | **0 cpm** | ○＋品質フラグ |

`skipped_large_unreadable` は公式マニュアルの想定（大→小に単調に成績が低下する）の外にある。RA式は適用するが、必ず `LARGE_PRINT_FALLOFF` フラグを立て、目視確認を要求する。

### 4.3 採点細則（Q&A準拠）

- 自己訂正して正しく読み直した文字は**誤りに数えない**。訂正に要した時間は $t_i$ に**含める**。
- MNREAD-J で1行（10文字）を丸ごと飛ばした場合、**10文字の誤り**として扱う。
- 同じ行を読み直して正しく読めた場合は誤りに数えないが、読み直し時間を含める。

---

## 5. 算出式

### 5.1 読書速度

$$v_i = \frac{60\,(n_0 - e_i)}{t_i} \quad [\mathrm{cpm}]$$

- $e_i = n_0$ のとき $v_i = 0$（厳密に0）。
- $e_i > n_0$、$t_i \le 0$、非有限値は**入力エラー**。0に丸めない（§6）。
- 未試行は 0 cpm ではなく**欠測**。

### 5.2 距離補正

$$\Delta L = \log_{10}\!\left(\frac{D_0}{D}\right), \qquad L_i^{c} = L_i + \Delta L$$

- $D_0 = 30$ cm。
- 補正は **RA・CPS・曲線の横軸に適用する。読書速度 cpm には適用しない。**
- 行ごとに距離が異なる場合は $\Delta L_i = \log_{10}(D_0/D_i)$ を行ごとに適用する。距離が記録されずに変動した場合は品質不良として扱う。

### 5.3 読書視力 RA

#### 5.3.1 公式形

$$RA_{\mathrm{chart}} = 1.4 - 0.1N + \frac{E}{d}, \qquad d = \begin{cases}300 & \text{(J)}\\ 240 & \text{(Jk)}\end{cases}$$

$$RA_{\mathrm{corrected}} = RA_{\mathrm{chart}} + \Delta L$$

$N$ は最上段から数えた、読んだまたは読もうとした読み材料数。上位を省略して途中から開始した場合、省略した上段は**正読（誤り0）として $N$ に含める**。

#### 5.3.2 一般形（実装はこちらを用いる）

$$RA_{\mathrm{chart}} = L_{\min} + \frac{h\,E}{n_0}$$

$L_{\min}$ は最後に試行した行のチャート表示サイズ、$h = 0.1$。上端1.3の標準チャートでは $L_{\min} = 1.4 - 0.1N$ となり公式形と一致する。将来のチャート変更に耐えるため一般形で実装する。

#### 5.3.3 不変性（テストで担保する性質）

全文字を読み損じた読み材料（$e = n_0$）を $N$ と $E$ の双方に算入しても、算入しなくても **RAは変わらない**。

$$\Delta RA = -0.1 + \frac{0.1 \, n_0}{n_0} = 0$$

したがって「不読行を数えるか」は RA に影響しない。ただし**曲線と MRS/CPS には影響する**ため、状態としては必ず区別して保持する。

> この不変性は原典の記述のゆれを説明する。マニュアル §2.3(7) は「読んだ**あるいは読もうとした**文章数」を数えるとするが、§4.5 の計算例は 0.5 logMAR で1文字も読めなかった読み材料を $N$ に含めず $N=8$、$E=59$ としている。どちらの解釈でも RA は 0.796667 で一致するため、原典は両者を区別する必要がなかった。

#### 5.3.4 打ち切り

読めない行まで到達せずに検査を終えた場合、RA は下限値（打ち切り）として扱い、`censored: true` を立てる。「RA = ○○ 以下」と表示し、確定値として出さない。

### 5.4 最大読書速度 MRS

公式マニュアルは定義本文（速度の平均）と計算例（時間の平均から換算）が一致していない。**3値を並列に出力し、実装が黙って一方を選ばない。**

| 出力 | 定義 | 扱い |
|---|---|---|
| `mrs_arithmetic` | $\dfrac{1}{|P|}\sum_{i \in P} v_i$ | **標準値**（定義本文に忠実） |
| `mrs_pooled` | $60 \dfrac{\sum_i (n_0 - e_i)}{\sum_i t_i}$ | 補助値 |
| `mrs_legacy_mean_time` | $60 \dfrac{n_0}{\bar t}$ | 公式計算例互換。**$P$ 内の全項目が $e_i = 0$ のときのみ有効**、それ以外は `null` |

$P$ は CPS 以上のプラトー集合（§5.5）。付随して `mrs_sd_cpm`（標本SD）と `mrs_n`（点数）を返す。マニュアル §3.3 は標準偏差の併用を明示的に勧めている（「平均値だけでなく、標準偏差も使えば、その患者がベストの状態で読書をしたときの読書速度の信頼区間を求めて、より客観的にエイドの妥当性を評価することもできます」）。

`mrs_arithmetic` と `mrs_legacy_mean_time` の差が設定値（既定5%）を超えたら `MRS_METHOD_DISAGREEMENT` フラグを立てる。

> **原典の扱い**: マニュアル §3.3 の定義本文は「臨界文字サイズ以上のときの読書速度の平均値」であり `mrs_arithmetic` に対応する。§4.4 の計算例が平均時間方式を採るのは矛盾ではなく、**その場で「読み間違いを1つもしていないので」と条件を明示している**ため。この条件が満たされないときに平均時間方式を適用してはならない、という本仕様の規則は原典の記述と整合する。

### 5.5 臨界文字サイズ CPS

定義: **最大読書速度で読むことのできる最小の文字サイズ**。logMAR は値が大きいほど文字が大きいため、プラトー集合 $P$ のうち数値的に最小の logMAR が CPS となる。

$$CPS = \min\{L_i^{c} : i \in P\}$$

#### 5.5.1 算出法IDと主値

**CPS値は算出法IDを伴わずに出力してはならない。** 「CPS = 0.6 logMAR」単独の行を、画面・EMRテキスト・書き出しファイルのいずれにも出さない。

| 算出法ID | 内容 | 位置づけ |
|---|---|---|
| `manual_visual_2002` | 検者がグラフ上で選択したプラトー | **臨床の主値** |
| `plateau_sdev_v1` | 連続区間の探索（§5.5.2）。本プロジェクト固有の定義 | 参考値 |
| `expdecay_80` / `_90` / `_95` | 指数減衰フィットの MRS 80/90/95% 到達サイズ | 参考値（Phase 2） |

主値は常に `manual_visual_2002`。自動値は併記するが置換しない。検者が自動値を採用した場合も、採用操作を記録する。

**出荷時の有効算出法**（`enabledCpsMethods` の既定）は `manual_visual_2002` / `plateau_sdev_v1` / `expdecay_90` の3つとする。§7 の `CPS_METHOD_DISAGREEMENT` は算出法どうしの食い違いで発火するため、**レビュー発火率は有効算出法の集合に依存する**。したがって受け入れ基準（とくに「誤った CPS を無警告で出さない」）は**この出荷構成で検証しなければならない**。画面に出ない構成で証明した基準は出荷の保証にならない。`expdecay_80` / `_95` を既定に含めないのは、§7 の比較対象が `expdecay_90` のみで、追加してもレビュー発火が1件も変わらないため（合成600本で確認）。

**暫定主値の降格順は `manual_visual_2002` → `plateau_sdev_v1` で打ち切る。** 検者の目視判定がなく SDev 法も推定不能な場合、自動 CPS は表示しない。指数フィットをここへ昇格させない（ADR-0015）。SDev 法が推定不能になる主因は欠測段であり（§5.5.2 は欠測段をまたがない）、欠測を無視して曲線全体にフィットする手法でその穴を埋めることは、§5.5.2 が拒んだ推定を別の算出法IDで行うことに等しい。参考値としての併記は妨げない。

#### 5.5.2 プラトー探索（`plateau_sdev_v1`）

原典に細則がないため、以下を本仕様として固定する（OPEN-3 の裁定）。反復的な貪欲拡張ではなく、**条件を満たす区間を宣言的に選ぶ**定義を採る。開始位置や拡張順序に依存せず、結果が一意に定まるためである。

**核** $P_0$ は、測定値のある隣接2段（欠測段をまたがない）のうち平均速度が最大の組。同値なら小さい文字側を採る。

**プラトー帯**: 核から水準 $\mu = \bar v_{P_0}$ とばらつき $s = \max(s_{P_0},\ 0.05\,\mu)$ を求め、

$$v \ge \mu - 1.96\,s$$

を満たす点を「同じプラトーに属する」とみなす。

> ばらつきの下限 5% は原典の実測に合わせたもの。マニュアル §4 の測定例のプラトー（404.49 / 436.89 / 394.74 cpm）は $s = 22.07$、$\mu = 412.04$ で比にして 5.4% である。核は2点しかなく標本 sd がプラトー全体のばらつきを過小評価しうるため、この下限がないと拡張が早すぎる段階で止まる。読書時間の0.1秒量子化に由来する1〜2%はこれに含まれる。

**拡張**: 核から大側・小側の両方向へ、連続する段をたどりながら帯の内側にある点を取り込む。帯を外れた点、または欠測段に当たった時点でその方向を打ち切る。

**再推定**: 得られた区間から帯を求め直し、核から張り直す。収束するまで（上限10回）繰り返す。核が2点しかない不安定さを、区間全体のばらつきで補正するため。区間は広がるだけでなく狭まることもある。

**CPS** は得られた区間の最小 logMAR。有効速度点が3点以下の場合は §5.5.4 により推定不能とする。

**固定の百分率を用いない理由**: 「MRS の85%」のような固定比率にすると、プラトーが安定した患者にも不安定な患者にも同じ物差しを当てることになる。前者では過度に緩く、後者では過度に厳しい。帯を患者自身のばらつきから決める。

**原典との対応**: この定義はマニュアル §4 の測定例で、検者の目視判定（CPS = 1.1 logMAR、プラトー = 1.3/1.2/1.1）と**一意に一致する**ことを確認している。

**名称について**: 算出法IDを `sdev_1.96` としないのは、本定義が `mnreadR` の実装を再現するものではなく、本プロジェクト固有の裁定を含むためである。`mnreadR` 2.1.7 との実際の差異は Phase 5 で測定する。

#### 5.5.3 0 cpm の扱い

指数減衰・two-limb は速度を対数変換するため 0 cpm を投入できない。

- 原データと線形グラフでは 0 cpm を**保持する**。
- フィット時に微小値 $\epsilon$ へ**置換しない**。
- 対数回帰から除外し、**除外件数を返す**。
- 正速度が小文字側またはプラトー側の一方にしか存在しない場合、CPS を**推定不能**とする。

#### 5.5.4 推定不能

以下では CPS を数値として返さず `estimable: false` と理由を返す。

- 有効速度点が3点以下
- 隣接2サイズ以上のプラトーが存在しない
- フィットが収束しない、またはパラメータが探索境界に張り付いた
- CPS が実測 logMAR 範囲の外（`extrapolated: true`。数値を通常値として表示しない）

### 5.6 アクセシビリティ指標

英語版 ACC の正規化定数 200 wpm は英語母語話者から得られたものであり、**cpm に転用しない**。

日本語版では 1.3〜0.4 logMAR の名目10行の平均 cpm を**非正規化のまま**出力する。

$$\overline{v}_{1.3:0.4} = \frac{1}{10}\sum_{L = 0.4}^{1.3} v(L) \quad [\mathrm{cpm}]$$

- 表示名は「読書アクセシビリティ対象範囲平均速度」とし、**`ACC` と単独表示しない**。
- `acc_normalized` は常に `null`（日本語版の基準値が確立するまで）。
- 欠測の扱いは原著規則に従う: 読書視力限界により読めない → 0、大文字が読めない → 0、便宜上省略した → 次に実測された速度で補完。それ以外の補間は禁止。

### 5.7 単位換算

| 変換 | 式 | 出典 |
|---|---|---|
| 小数視力 | $VA = 10^{-L}$ | マニュアル §3.6、表B |
| M値 | $M = 10^{\,L - 0.4}$ | Q&A A7（式を明記）、マニュアル §4.3・図1a |
| ポイント（30cm） | $pt_{30}(L) = 1908\tan\!\left(10^{L}\cdot\frac{5}{60}\cdot\frac{\pi}{180}\right)$ | Q&A A4.3 |
| ポイント（距離D） | $pt_D(L) = pt_{30}(L)\cdot \dfrac{D}{30}$ | — |
| 物理高 | $h_{\mathrm{mm}} = pt \times \dfrac{25.4}{72}$ | — |
| 視角（日本語文字） | $\theta = 11.4 \times 10^{L}$ arcmin | Q&A A4.2（**OPEN-5** 参照） |

換算にはすべて**距離補正後の logMAR** を用いる。

M は Magnification の頭字であり、新聞を読むのに必要な倍率を表す（Q&A A7）。マニュアル表A は M への距離補正を乗数 $30/D$ として与えるが、これは logMAR 補正 $\log_{10}(30/D)$ と等価であり、補正後 logMAR から上式で求めれば足りる。

> マニュアル §1.2 は「1M は現在の新聞の活字サイズより少し小さめになってしまっている」と注意している。倍率の目安として提示する際はこの点に留意する。

ポイント値は MNREAD-J の文字設計（明朝体）に対応した**相当値**であり、任意のフォントの実際の字面高と一致しない。表示は必ず「MNREAD-J相当」と明記する。

支援用の推奨サイズを示す場合、CPS そのものと支援余裕を分離する。

$$pt_{\text{下限}} = pt(CPS), \qquad pt_{\text{上限}} = pt(CPS + r), \quad r = 0.1\ (\approx 1.26\text{倍})$$

CPS は「快適さを保証する推奨サイズ」ではなく「最大速度を支える最小サイズ」であるため、この区別を表示上も崩さない。

Jk から漢字かな交じり教材を推定する場合、$L_{\text{support}} = CPS_{Jk} + 0.1$ を参考値として併記できる。

### 5.8 判読ゾーン（患者・支援者向け表示）

A4 レポートで文字サイズを「不可 / 努力 / 快適」の3帯に分けて示す（ADR-0013）。**原典に規定がない本プロジェクト固有の裁定である**（OPEN-7）。

境界は **RA と CPS の2点のみ**に置く。$L^c$ を距離補正後 logMAR として、

| ゾーン | 範囲 | 根拠 |
|---|---|---|
| `unreadable`（不可） | $L^c < RA$ | RA は「読める最小の文字サイズ」であり、これを下回るサイズは読めない（§5.3） |
| `effortful`（努力） | $RA \le L^c < CPS$ | 読めるが最大読書速度に達しない範囲（§5.5 の定義の裏返し） |
| `comfortable`（快適） | $CPS \le L^c$ | 最大読書速度で読める範囲 |

**支援余裕 $r = 0.1$ logMAR はゾーンの境界ではない。** $CPS \le L^c < CPS + r$ の帯は最大読書速度が出ている範囲であり、これを「努力」に落とすと事実と表示が食い違う。余裕は §5.7 の推奨サイズ範囲 $[pt(CPS),\ pt(CPS+r)]$ として**別枠**に出す。

各境界は測定距離での MNREAD-J 相当ポイント（§5.7）を併記する。ゾーンは CPS に依存するため、**CPS が推定不能なら判読ゾーンを出力しない**（`null`）。RA が null の場合も同じ。

**退化の扱い**: 誤りが多い症例では $RA > CPS$ となりうる（RA は $-0.1 + E/d$ の誤り項で押し上げられるため）。このとき「努力」ゾーンは空になる。**値を入れ替えたり丸めたりせず**、空のゾーンをそのまま返し、`RA_ABOVE_CPS` フラグを立てて目視確認を要求する（§7）。

ゾーンは患者・支援者に示す**目安**であり、診断ではない（§1.3）。レポートには CPS の算出法 ID を必ず併記する（ADR-0006）。

---

## 6. 入力検証

**エラーは拒否する。黙って丸めない。**

| コード | 条件 | 重大度 |
|---|---|---|
| `ERR_TIME_NOT_POSITIVE` | $t_i \le 0$ または非有限 | error |
| `ERR_ERRORS_OUT_OF_RANGE` | $e_i < 0$ または $e_i > n_0$ | error |
| `ERR_ERRORS_NOT_INTEGER` | $e_i$ が非整数 | error |
| `ERR_DISTANCE_NOT_POSITIVE` | $D \le 0$ または非有限 | error |
| `ERR_DUPLICATE_SIZE` | 同一 logMAR が複数行（反復試行の明示なし） | error |
| `ERR_STATE_FIELD_MISMATCH` | 状態と time/errors の有無が矛盾 | error |
| `WARN_SEQUENCE_REVERSED` | 小→大の順で実施 | warning（整列して解析） |
| `WARN_DISTANCE_VARIED` | 行別距離が不揃い | warning |
| `WARN_IMPLAUSIBLE_SPEED` | 速度が生理的範囲外 | warning（記録は削除しない） |

error が1件でもある場合、解析結果を返さず検証結果のみを返す。部分的な結果を返さない。

---

## 7. 品質フラグ（目視レビュー要求）

以下のいずれかで `requiresReview: true` とし、該当フラグを返す。

| フラグ | 条件 |
|---|---|
| `TOO_FEW_POINTS` | 有効速度点が3点以下 |
| `NO_PLATEAU` | 隣接2サイズ以上のプラトーがない |
| `CPS_AT_BOUNDARY` | CPS が最初または最後の実測サイズ |
| `CPS_METHOD_DISAGREEMENT` | 目視/SDev/フィットの CPS が 0.2 logMAR超乖離 |
| `MRS_METHOD_DISAGREEMENT` | 算術平均と平均時間方式が既定5%超乖離 |
| `HIGH_INFLUENCE_POINT` | 1点の除外で CPS が 0.1 logMAR超変化 |
| `LARGE_PRINT_FALLOFF` | 大文字側で速度が低下（非単調） |
| `FIT_NOT_CONVERGED` | フィット非収束・境界張り付き |
| `CPS_EXTRAPOLATED` | CPS が実測範囲外 |
| `RA_CENSORED` | 読めない行まで到達していない |
| `ZERO_SPEED_HANDLING_DIVERGES` | 0 cpm の扱いで算出法間の結果が変わる |
| `PLATEAU_GAP` | CPS より大きい文字サイズの実測点がプラトーから外れている（外れ値・二重プラトー） |
| `IMPLAUSIBLE_VALUE` | 読書時間・速度が生理的範囲を外れている（`WARN_IMPLAUSIBLE_SPEED` に対応） |
| `RA_ABOVE_CPS` | $RA > CPS$。判読ゾーンの「努力」が空になる退化（§5.8） |
| `OVERRIDE_REASON_MISSING` | 検者の目視判定が自動値と異なるプラトーを指しているのに、上書き理由が記録されていない（§8.4） |

`CPS_METHOD_DISAGREEMENT` の比較対象は目視・SDev・指数フィット90% の3つに限る。指数フィットの 80/90/95% は閾値が異なる以上ずれて当然であり、同列に並べると常に乖離と判定されてフラグが機能しなくなる。90% を代表とするのは、評定者との一致がこの閾値で最も高いとする報告（Baskaran 2019）による。

正常な曲線での誤発火率は 20% 未満を目標とする（Phase 2 で閾値を調整）。

---

## 8. 出力

### 8.1 必須の付随情報

すべての解析結果に以下を含める。欠けた出力を生成しない。

- `specVersion` / `algorithmVersion`
- 各 CPS 値の算出法ID、各 MRS 値の方式ID
- `viewingDistanceCm` と `distanceCorrectionLogMAR`
- `qualityFlags`
- 検者が自動値を上書きした場合は `overrideReason`

### 8.2 出力先

**3系統とも §8.1 を満たす。** 満たせない出力を生成しない。

#### 8.2.1 電子カルテ用テキスト

貼り付けて使う定型文。**含めるものを固定する。**

- チャート種別・チャート版・測定眼・極性・実施順
- 測定距離と距離補正値（符号つき）
- RA（打ち切りなら「以下」と明記）
- CPS — **算出法 ID を同じ行に置く**。「CPS 0.6 logMAR」だけの行を作らない（ADR-0006）
- MRS — **3方式すべて**を方式 ID つきで（ADR-0005）。1方式だけの行を作らない
- CPS 相当の M 値・MNREAD-J 相当ポイント・小数視力
- 推奨文字サイズ範囲（下限 = CPS 相当、上限 = 支援余裕込み。§5.7 のとおり分けて書く）
- 品質フラグと、上書きした場合はその理由
- `specVersion` / `algorithmVersion` と検証状況

自動値を主値として採った場合（検者がまだ目視判定していない場合）、**その事実を文中に明示する**。

#### 8.2.2 A4 患者・支援者向けレポート

- 判読ゾーン3帯（§5.8）。境界は logMAR と MNREAD-J 相当ポイントの両方で示す
- 推奨文字サイズ範囲を**ゾーンとは別枠**に置く（§5.7）
- 読書速度曲線（§8.3）をベクタのまま印刷する
- 患者に渡す紙であるため、**氏名は入れない**。ID・実施日・測定距離までとする
- 「参考値であり診断ではない」旨と、ポイント値が MNREAD-J 相当であってフォント依存である旨を必ず記載する

印刷は `window.print()` と `@media print` による。ラスタ化を伴う手段を用いない（PLAN §2）。

#### 8.2.3 生データの書き出しと読み込み

- **氏名欄を設けない。** ID・年齢・性別・実施日まで
- JSON は解析結果を**丸めずフル精度で**出す（ADR-0003）。表示用の丸めは書き出しに持ち込まない
- CSV は1行 = 1読み材料とし、状態・時間・誤り数・速度・チャート logMAR・補正後 logMAR を含める。セッション情報と解析値はヘッダ行群として先に置く
- 書き出しは検者の明示操作でのみ発生する。自動保存・自動送信を実装しない

**JSON は解析をやり直せる形でなければならない。** 値だけを書き出すと、後から「なぜその CPS になったか」を再現できない。したがって次を必ず含める。

- `input`（セッション入力そのもの）
- `manualPlateau`（検者の目視判定。CPS・MRS はこの集合の関数である。ADR-0012）
- `selection.overrideReason`
- `specVersion` / `algorithmVersion`

**読み込み（インポート）の規則**

- 読み込みは検者の明示操作でのみ発生する。自動読み込み・ドラッグ＆ドロップによる暗黙の取り込みを実装しない
- 復元するのは**編集・再解析できる状態**であり、表示だけの再現ではない。プラトー選択・除外とその理由・上書き理由まで戻す
- **壊れたファイルは拒否する。読める部分だけ入れない。** 半分だけ入った検査は、入力途中の検査と見分けがつかない
- 値の妥当性は読み込み後に `validateSession()` が判定する。読み込み側は形式と段の照合までを見る
- `items` は実施順、行はチャート順であるため、**添字ではなく `chartLogMAR` の値で突き合わせる**。小→大で実施した記録も同じ行に着地する
- `specVersion` / `algorithmVersion` がアプリと異なるファイルは読むが、**警告する**。算出法が変わっていれば表示される値も変わりうる（§10）

### 8.3 グラフ

グラフの軸は **横軸 = 距離補正後 logMAR、縦軸 = 読書速度 cpm（常用対数目盛）** とする（ADR-0011）。公式マニュアル図4、ミネソタ大学の原著論文および MNREAD アプリのいずれもこの作図であり、曲線フィットが行われる空間とも一致する。

**目盛は対数のみとし、線形表示は用意しない。** 検者ごと・回ごとに異なる形の曲線で目視判定できてしまう状態は、再現性を損なうためである。

> 算出は日本語版一次資料にのみ従う（§1.1、ADR-0001）。作図の設計に英語版文献を参照してよいのは、軸の取り方が数値を変えないためである。判定基準は「その参照が数値を変えるか」。

**0 cpm の点は捨てない。** 対数目盛は 0 を表現できないため、対数領域の下に軸を分断した専用の帯を設けて描き、`0` と明示する。0 cpm は測定された事実であり（ADR-0002）、ロービジョン例では読書視力の境界を決める点になる。

- 0 cpm 点を通る折れ線は引かない。対数領域と帯をまたぐ線分は傾きに意味を持たないため、帯の点は独立した印として描く。
- 正速度が 1 cpm を下回る場合、対数目盛の下端を実測値に応じて桁単位で延ばす。目盛の外へ丸め込まない。
- 表示上の扱いと、対数回帰から 0 cpm を除外する規則（§5.5.3）は別である。除外件数の表示は §5.5.3 のとおり。

**判定結果の描画**（Phase 4）。主グラフには次を重ねる。いずれも算出結果であり、入力エラーがある間は描かない（ADR-0004）。

- 選択されたプラトー点の強調
- CPS の縦線。**算出法 ID を凡例に添える**
- MRS の水平線。プラトーの範囲にのみ引き、外へ延ばさない（MRS はプラトー内の平均であって全域の値ではない）

#### 8.3.1 副グラフ（読書時間）

原典 図3 に対応する副表示を設ける。**横軸 = 距離補正後 logMAR、縦軸 = 読書時間（秒、常用対数目盛）。**

読書時間は検者が実際に記録した量であり、速度は そこから導いた量である。原典が図3で時間軸を示しているのは、記録した値そのものの形を確認できるようにするためであり、この副表示は転記誤りの発見に効く。

- 時間が欠測の点（`presented_time_missing` など）は描かない。0 cpm の点は時間を持たないため主グラフの帯にのみ現れる
- 副グラフに判定線（CPS・MRS）は引かない。判定は主グラフで行う

### 8.4 検者の判定操作と監査記録

臨床の主値は検者の目視判定である（ADR-0006）。判定 UI が受け取る入力は**プラトー点の集合 $P$ のみ**とする（ADR-0012）。

- CPS は $P$ の最小 $L^c$、MRS は $P$ 内速度の平均であり、いずれも $P$ から導く。**$P$ と独立に CPS・MRS を上書きできる操作を設けない。** 設ければ、出力の CPS と MRS が別のプラトーを指す状態を作れてしまう
- 外れ値は「点の削除」ではなく「除外として記録」する。除外理由を残す（`PlateauSelection.exclusionReasons`）
- 検者が自動値（`plateau_sdev_v1`）をそのまま採用した場合も、採用したという操作を記録する
- 目視判定が自動値と**異なる**プラトーを指す場合、`overridesAutomatic: true` とする。このとき `overrideReason` が空なら `OVERRIDE_REASON_MISSING` を立てる（§7）

---

## 9. 丸めと表示

**内部計算では一切丸めない。** 倍精度のまま保持し、表示直前にのみ丸める。

| 値 | 保存 | 表示 |
|---|---|---|
| logMAR 各種 | フル精度 | 小数第2位 |
| RA | フル精度 | 小数第2位（旧記録比較時のみ第1位） |
| 速度 | フル精度 | 整数 cpm |
| 距離補正 | フル精度 | 小数第2位（符号付き） |

RA の誤り1文字あたりの分解能は J で約 0.00333、Jk で約 0.00417 logMAR。表示桁がこれを下回る点に留意する。

---

## 10. アルゴリズムID と版

`algorithmVersion` は semver とし、**算出結果が変わりうる変更で必ず上げる**。画面と全出力に表示する。

自動推定を用いた出力では、モデル・閾値・ライブラリ版を併せて保存する。

---

## 11. 未解決事項（OPEN）

| ID | 内容 | 方針 | 状態 |
|---|---|---|---|
| ~~OPEN-1~~ | M値の定数 | **解決**。Q&A A7 が $M = 10^{L-0.4}$ を明記。マニュアル §4.3（1.1 logMAR = 5M）、§1.2（4M ≒ 28pt）、図1a のチャート印刷値と一致 | 2026-07-31 解決 |
| **OPEN-2** | `skipped_large_unreadable` の RA 式への算入可否（マニュアルの想定外） | $N$ と $E$ に算入（RA は §5.3.3 により不変）。品質フラグで目視確認を要求。Q&A Q8 は網膜色素変性症等でこの型の曲線が生じることを認めている | Phase 5 |
| ~~OPEN-3~~ | SDev法の細則が原典で規定されていない | **裁定済**。§5.5.2 に宣言的定義（連続区間＋内部凝集性＋外部分離、1.96SD）を明記。マニュアル §4 測定例の目視判定と一致することを確認 | 2026-07-31 裁定。`mnreadR` 2.1.7 との差異検証は Phase 5 |
| **OPEN-4** | `MRS_METHOD_DISAGREEMENT` の閾値5%に臨床的根拠はない | 合成曲線と実測20〜30例で調整 | Phase 5 |
| **OPEN-6** | レビュートリガの発火率。合成曲線ではノイズと量子化を重ねた族で30%が発火する（clean な曲線は0%）。`HIGH_INFLUENCE_POINT` の 0.1 logMAR、`PLATEAU_GAP`、`MIN_RELATIVE_SD` の 5% はいずれも合成データでの暫定値。**`cpsDisagreementThresholdLogMAR` の 0.2 も同様**で、原典 §4 の測定例は差 0.206（SDev 1.401 対 指数フィット 1.607）でこれをわずかに超え、出荷既定でフラグが立つ。指数フィットの外挿値が食い違いの主因（既定構成の発火55件中53件） | 過剰警告側に倒す（臨床の主値が目視判定である以上、安全な失敗方向）。実測20〜30例で較正する。閾値を上げる前に、外挿値を比較に含めるかを併せて決めること（現状は含める。外すと OPEN-9 の防護が消える） | Phase 5 |
| **OPEN-7** | 判読3ゾーンの境界（§5.8）。原典に規定がなく、境界を RA と CPS に置くのは本仕様の裁定である。「努力」ゾーンの下端を RA そのものに置くと、RA 近傍は実際には数文字を拾える程度でしかない例がある | 患者・支援者向けの目安表示に限り、EMR テキストと生データには持ち込まない。実測20〜30例で ORT の体感と突き合わせる | Phase 5 |
| **OPEN-5** | 視角係数の Q&A 内不整合。A4.2 は $\theta = 11.4 \times 10^L$ arcmin を示すが、A4.3 のポイント換算式が含意する係数は $5 \times 2.2437 = 11.22$ で約1.6%異なる | 11.4 を採用。視角は情報表示のみに用い、pt・M・臨床値はこの定数に依存しない | 低優先 |
| **OPEN-8** | §5.5.2 の核は「平均が最大の隣接2点」であるため、単一の高い外れ値があれば必ずそれを含む組が核になる。核は2点なので標本 sd が外れ値の逸脱量だけで決まり、帯が過大になって真の CPS より下まで飲み込む。合成 `single_high_outlier` 族で 40/40 発生し、CPS が 0.4（真値 0.6）。**原典 §3.2 の「読書速度が落ちはじめる直前の文字サイズ」に反する** | 修正する。方向に選択肢がある（核を3点にする／核 sd に上限／外れ値を核候補から除外／再推定の順序変更）ため、他14族への影響測定と mutation checks の再実行を伴う独立の変更として行う。`requiresReview` は 40/40 発火しており silent-wrong ではない | Phase 5（`docs/mnreadr-comparison.md` E-1）|
| ~~OPEN-9~~ | `requiresReview` の発火率が有効算出法の集合に依存し、出荷の既定構成が受け入れテストの構成より弱かった | **解決**。(c) を採る。§5.5.1 に出荷構成（目視・SDev・`expdecay_90`）を明記し、既定に `expdecay_90` を追加。合成受け入れテストの最重要基準を出荷構成でも走らせる。あわせて暫定主値の降格順を SDev で打ち切る（ADR-0015） | 2026-08-01 解決（`docs/mnreadr-comparison.md` F-1）|

OPEN 項目は、解消するまで実装内で `// OPEN-n` コメントで参照し、放置を防ぐ。
