# MNREAD-J / Jk Web解析ツール 基本仕様書

## 1. システム概要
* **目的**: ロービジョン外来・診察室において、MNREAD-J / Jk チャートの測定データ（読破時間・エラー数）から読書視力、臨界文字サイズ、最大読書速度等を自動解析し、電子カルテ記録用テキストおよび患者説明用レポート（PDF）を即座に生成する。
* **ターゲットユーザー**: 医師、視能訓練士（ORT）、視覚障害支援専門員、特別支援教育関係者。
* **動作環境**: ブラウザ上で動作するSPA（Single Page Application）/ PWA。iPad等のタブレットおよびPC端末。
* **セキュリティ・データ保護**: 患者の個人情報（PHI）漏洩リスクを防止するため、**すべての計算・描画処理をブラウザローカル（クライアントサイド）で完結**し、外部サーバーへのデータ送信は行わない。

---

## 2. 機能仕様

### 2.1 入力機能（スピーディ入力UI）
1. **基本設定条件**
   * **使用チャート**: `MNREAD-J（漢字かな交じり文）` / `MNREAD-Jk（ひらがな単語）`
   * **表示タイプ**: `白地に黒文字（通常）` / `黒地に白文字（反転）`
   * **検査眼**: `右眼` / `左眼` / `両眼`
   * **測定距離**: デフォルト `30 cm`（15cm, 20cm, 50cm等、任意数字の入力・選択可能）
   * **被験者情報**: ID、年齢、性別、測定日（任意入力）
2. **スコアシート型テーブル入力**
   * 1.3 logMAR 〜 -0.5 logMAR（全19段階）のリスト形式UI。
   * **読破時間（秒）**: 小数第1位まで入力（例: `4.5`）。
   * **エラー数（文字）**: 読み誤り・読み飛ばしの文字数を入力。
   * **入力省略補正**:
     * 読み始めた段より上の未測定サイズは「エラー0・標準時間」として全問正解扱い。
     * 不読（読めなかった）段は時間 `0` または未入力で処理し、読書速度 0 cpm とする。
   * **操作性**: テンキーおよび `Tab` キー遷移に最適化（キーボードのみで全項目入力可能）。

---

### 2.2 解析・アルゴリズム仕様

#### ① 読書速度（cpm: characters per minute）
* **MNREAD-J**: $\text{cpm} = \frac{30 - \text{エラー数}}{\text{読破時間(秒)}} \times 60$
* **MNREAD-Jk**: $\text{cpm} = \frac{24 - \text{エラー数}}{\text{読破時間(秒)}} \times 60$

#### ② 測定距離の補正（logMAR加減算）
* **補正値 ($\Delta \text{logMAR}$)**: $\log_{10}\left(\frac{30}{\text{測定距離(cm)}}\right)$
  * 例: 15cmでの測定の場合、$+=0.30 \text{ logMAR}$ の補正。

#### ③ 読書視力 (Reading Acuity: RA)
* **計算式**:
  * $J$: $\text{RA} = 1.4 - (\text{読了文章数} \times 0.1) + \left(\frac{\text{全エラー数}}{300}\right) + \Delta \text{logMAR}$
  * $Jk$: $\text{RA} = 1.4 - (\text{読了文章数} \times 0.1) + \left(\frac{\text{全エラー数}}{240}\right) + \Delta \text{logMAR}$
* **換算表示**: logMAR値および小数視力換算値を併記。

#### ④ 最大読書速度 (Maximum Reading Speed: MRS) & 臨界文字サイズ (Critical Print Size: CPS)
* **自動算定ロジック**:
  * 読書速度が一定範囲（プラトー）にある区間の平均値を **MRS** と定義。
  * MRSから一定割合（例: 80%〜85%）を下回る直前の文字サイズを **CPS** として自動抽出。
* **Jk ➔ J 臨床換算（Jk使用時）**:
  * MNREAD-Jkで測定した場合、実生活の漢字かな交じり文評価用に CPS（logMAR）に $+0.1 \text{ logMAR}$（拡大倍率にして約1.26倍）を加算した「実用推奨値」を参考表示。
* **インタラクティブ手動修正機能**:
  * データゆらぎ対策として、グラフ上の「MRS（横スライダー）」および「CPS（縦スライダー）」をドラッグ操作で手動調整可能とする。

---

### 2.3 出力機能

#### ① グラフ描画（マニュアル図3準拠）
* **両対数風プロットグラフ**:
  * 横軸: 文字サイズ $\text{logMAR}$ (1.3 〜 -0.5)
  * 左縦軸: 読破時間（秒 / 対数軸）
  * 右縦軸: 読書速度（cpm / 対数軸）
  * 測定距離補正済みのスケール軸表示。

#### ② 電子カルテ用テキストコピー（1タップ）
* クリック一発でクリップボードにコピー可能なテキスト出力フォーマット。

```text
【MNREAD-J 読書能力評価】
測定日: 2026/07/31 | 検査眼: 両眼 | 距離: 15cm (補正: +0.3 logMAR)
・最大読書速度 (MRS): 380 cpm
・臨界文字サイズ (CPS): 1.4 logMAR (8.0M / 0.04相当) [新聞(1M)に対し約8倍〜10倍拡大]
・読書視力 (RA): 1.1 logMAR (0.08相当)
・所見: 1.1 logMAR以下で急速に速度低下。拡大読書器/ルーペ(8倍以上)の利用を推奨。
```

#### ③ 患者・家族・指導者向け説明レポート（A4・1ページ PDF/印刷）
* **ビジュアル表示**:
  * カラーグラフ（見やすい高コントラスト配色）。
  * 読書領域のシグナル表示:
    * 🟢 **快適に読めるサイズ**（CPS以上）：読書・学習に最適な文字サイズ。
    * 🟡 **努力すれば読めるサイズ**（RA〜CPS）：短時間の確認・看板等。
    * 🔴 **読めないサイズ**（RA未満）。
  * **環境調整・AT機器提案**:
    * 「新聞や一般的な本を読むのに必要な倍率：約 〇 倍」
    * 「推奨されるタブレット/PCのフォントサイズ：〇〇 pt」

---

## 3. 非機能要件・技術選定案

1. **フロントエンド技術スタック**
   * **フレームワーク**: React / Next.js または Vue.js / Vite (SPA構成)
   * **グラフ描画ライブラリ**: Chart.js (Chartjs-plugin-annotation) または D3.js / Plotly.js
   * **PDF出力**: `html2pdf.js` または `jspdf` + `html2canvas`
2. **PWA（Progressive Web App）対応**
   * Service Workerを実装し、オフライン（院内LAN/完全オフライン端末）環境でも動作可能とする。
3. **アクセシビリティ**
   * WCAG 2.1 AA準拠（高コントラストモード対応、フォントサイズ変更対応）。
